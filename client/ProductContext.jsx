import React, { useState } from 'react';

const ProductContext = React.createContext(null);

export { ProductContext };

// find error in console
// where did that occur
// go to that line in the code via search
// create a TODO: